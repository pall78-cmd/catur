import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chessai.app',
  appName: 'Chess AI',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
